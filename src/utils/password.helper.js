
export const hashPassword = async (password = '') => {
    try {
    } catch (error) {
        throw error;
    }
}

export const compareHashedPassword = async (passwordCandidata) => {
    try {
     
    } catch(error) {
        throw new Error(error);
    }
}